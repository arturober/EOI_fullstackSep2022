spring:
    datasource:
        url: jdbc:mariadb://localhost:3306/gestion_eventos_login
        username: root
        password: 
        driver-class-name: org.mariadb.jdbc.Driver
    web:
        resources:
            static-locations: file:public

logging.level.org.springframework.jdbc.core: TRACE
